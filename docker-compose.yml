version: '3.8'
services:
  app:
    build: .
    image: django_project 
    container_name: django_container
    volumes:
      - .:/django
    ports:
      - 8000:8000
    command: "python manage.py runserver 0.0.0.0:8000"


    

    